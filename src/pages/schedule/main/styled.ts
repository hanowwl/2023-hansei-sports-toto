import styled from '@emotion/styled';

export const TagListRow = styled.div`
  display: flex;
  flex-direction: column;

  gap: 0.6rem;
`;

export const ScheduleRow = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1.2rem;
`;
